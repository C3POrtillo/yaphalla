export const exclusionClasses = ['export-ignore'] as const;

export const labels = {
  sm: {
    copy: 'Copy',
    export: 'Save',
  },
  base: {
    copy: 'Copy to Clipboard',
    export: 'Save to PNG',
  },
} as const;
